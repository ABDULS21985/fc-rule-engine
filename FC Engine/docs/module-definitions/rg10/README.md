# RG-10 Module Definitions

This directory contains production JSON template-definition files for RG-10:

- `insurance_naicom.json` (`INSURANCE_NAICOM`) - 12 templates
- `pfa_pencom.json` (`PFA_PENCOM`) - 12 templates
- `cmo_sec.json` (`CMO_SEC`) - 13 templates
- `dfi_cbn.json` (`DFI_CBN`) - 12 templates
- `imto_cbn.json` (`IMTO_CBN`) - 12 templates

Total across RG-10 definitions:

- Templates: 61
- Fields: 2,194
- Formulas: 494
- Cross-sheet rules: 97
- Inter-module data flows: 18

Traceability notes for source templates in `/Templates`:

- Each module definition includes `sourceSheetsCovered` metadata.
- Workbook data-dictionary names and XSD element names are represented in module fields (in addition to operational fields), so source artifacts are fully covered for import and audit.

Generated by:

`scripts/generate_rg10_module_definitions.py`

To regenerate:

```bash
cd "RegOS™"
python3 scripts/generate_rg10_module_definitions.py
```
