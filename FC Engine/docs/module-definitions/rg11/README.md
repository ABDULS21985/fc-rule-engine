# RG-11 Module Definitions

This directory contains production JSON template-definition files for RG-11:

- `fatf_eval.json` (`FATF_EVAL`) - 13 templates
- `esg_climate.json` (`ESG_CLIMATE`) - 13 templates

Total across RG-11 definitions:

- Templates: 26
- Fields: 1,491
- Formulas: 307
- Cross-sheet rules: 55
- Inter-module data flows (in-module): 0

Notes:

- FATF baseline metadata for 2021 MER and 2024 FUR is embedded in `FATF_TC` fields (`r01`-`r40`) via dedicated baseline fields and guidance metadata.
- FATF and ESG are receiving modules; inbound flow links are implemented by extending source module definitions (NFIU, DMB, CMO, DFI) to target FATF/ESG templates.
- FATF includes source coverage traceability from `/Templates/FATF_Mutual_Evaluation_Templates.xlsx` and `/Templates/FATF_ME_Schema_v1.0.xsd`.

Generated by:

`scripts/generate_rg11_module_definitions.py`

To regenerate:

```bash
cd "RegOS™"
python3 scripts/generate_rg11_module_definitions.py
```
