@* Generic status pill with colour variants *@

<span class="rg-cb-pill rg-cb-pill--@Variant">@Label</span>

@code {
    [Parameter] public string Label { get; set; } = "";
    [Parameter] public string Variant { get; set; } = "default";
}
