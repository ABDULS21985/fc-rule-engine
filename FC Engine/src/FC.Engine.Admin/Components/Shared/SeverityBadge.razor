@* Divergence severity badge — colour-coded *@

<span class="rg-cb-severity rg-cb-severity--@Severity.ToString().ToLowerInvariant()">
    @Severity
</span>

@code {
    [Parameter] public DivergenceSeverity Severity { get; set; }
}
