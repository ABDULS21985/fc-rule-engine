@page "/regulator/dashboards/examiner"
@attribute [Authorize(Policy = "RegulatorOnly")]
@inject FC.Engine.Admin.Services.Dashboards.StakeholderDashboardService DashboardService

<PageTitle>Examiner Dashboard</PageTitle>

<PageHeader Title="Examiner Dashboard"
            Subtitle="Assigned institutions, overdue reviews, pending examinations, and upcoming deadlines."
            BackUrl="/regulator/dashboards/governor">
    <PrimaryAction><button class="fc-btn fc-btn--primary" @onclick="ReloadAsync" disabled="@_loading">Refresh</button></PrimaryAction>
</PageHeader>

<div class="fc-page">
    <SectionErrorBoundary Title="Failed to load examiner dashboard" ExternalError="@_loadException" OnRetry="ReloadAsync">
        @if (_loading)
        {
            <div class="rg-dash-kpi-strip">
                @for (var i = 0; i < 4; i++)
                {
                    <div class="rg-dash-kpi"><SkeletonLoader Variant="SkeletonLoader.SkeletonVariant.Block" Height="88px" /></div>
                }
            </div>
        }
        else if (_data is null)
        {
            <EmptyState Context="data" Heading="No examiner data" Description="Refresh the Intelligence Hub to generate stakeholder data." />
        }
        else
        {
            <div class="rg-dash-kpi-strip">
                <div class="rg-dash-kpi">
                    <div class="rg-dash-kpi-value">@_data.AssignedInstitutions</div>
                    <div class="rg-dash-kpi-label">Assigned Institutions</div>
                </div>
                <div class="rg-dash-kpi">
                    <div class="rg-dash-kpi-value @(_data.OverdueReviews > 0 ? "rg-dash-kpi-value--red" : "rg-dash-kpi-value--green")">@_data.OverdueReviews</div>
                    <div class="rg-dash-kpi-label">Overdue Reviews</div>
                </div>
                <div class="rg-dash-kpi">
                    <div class="rg-dash-kpi-value">@_data.PendingExams</div>
                    <div class="rg-dash-kpi-label">Pending Exams</div>
                </div>
                <div class="rg-dash-kpi">
                    <div class="rg-dash-kpi-value @(_data.UpcomingDeadlines > 5 ? "rg-dash-kpi-value--amber" : "")">@_data.UpcomingDeadlines</div>
                    <div class="rg-dash-kpi-label">Upcoming Deadlines</div>
                </div>
            </div>

            @* Overdue Institutions *@
            <div class="fc-card fc-card--mb" style="margin-top:var(--space-4);">
                <div class="fc-card__header"><h2 class="fc-card__title">Overdue Institutions</h2></div>
                <div class="fc-card__body">
                    @if (_data.OverdueItems.Count == 0)
                    {
                        <p class="fc-text-muted">No overdue items. All institutions are current.</p>
                    }
                    else
                    {
                        <DataTable TItem="InstitutionScorecardRow" Items="@_data.OverdueItems" PageSize="20" Sortable="true" ShowExport="true" ExportFileName="examiner-overdue">
                            <ChildContent>
                                <DataColumn TItem="InstitutionScorecardRow" Title="Institution" SortKey="@(x => x.InstitutionName)">
                                    <CellTemplate><strong>@context.InstitutionName</strong></CellTemplate>
                                </DataColumn>
                                <DataColumn TItem="InstitutionScorecardRow" Title="Licence" Width="120px" SortKey="@(x => x.LicenceType)">
                                    <CellTemplate><span class="fc-badge fc-badge--neutral">@context.LicenceType</span></CellTemplate>
                                </DataColumn>
                                <DataColumn TItem="InstitutionScorecardRow" Title="Overdue" Width="90px" SortKey="@(x => x.OverdueObligations)">
                                    <CellTemplate><span class="fc-text-danger"><strong>@context.OverdueObligations</strong></span></CellTemplate>
                                </DataColumn>
                                <DataColumn TItem="InstitutionScorecardRow" Title="Due Soon" Width="90px" SortKey="@(x => x.DueSoonObligations)">
                                    <CellTemplate>@context.DueSoonObligations</CellTemplate>
                                </DataColumn>
                                <DataColumn TItem="InstitutionScorecardRow" Title="Capital" Width="90px" SortKey="@(x => x.CapitalScore)">
                                    <CellTemplate>@(context.CapitalScore?.ToString("F1") ?? "—")</CellTemplate>
                                </DataColumn>
                                <DataColumn TItem="InstitutionScorecardRow" Title="Incidents" Width="90px" SortKey="@(x => x.OpenResilienceIncidents)">
                                    <CellTemplate>@context.OpenResilienceIncidents</CellTemplate>
                                </DataColumn>
                                <DataColumn TItem="InstitutionScorecardRow" Title="Priority" Width="100px" SortKey="@(x => x.Priority)">
                                    <CellTemplate><span class="fc-badge @PriorityBadge(context.Priority)">@context.Priority</span></CellTemplate>
                                </DataColumn>
                                <DataColumn TItem="InstitutionScorecardRow" Title="Summary" SortKey="@(x => x.Summary)">
                                    <CellTemplate><span class="fc-text-muted">@context.Summary</span></CellTemplate>
                                </DataColumn>
                            </ChildContent>
                        </DataTable>
                    }
                </div>
            </div>

            @* Exam Queue *@
            <div class="fc-card fc-card--mb">
                <div class="fc-card__header"><h2 class="fc-card__title">Examination Queue</h2></div>
                <div class="fc-card__body">
                    @if (_data.ExamQueue.Count == 0)
                    {
                        <p class="fc-text-muted">No pending examinations.</p>
                    }
                    else
                    {
                        <DataTable TItem="InterventionQueueRow" Items="@_data.ExamQueue" PageSize="20" Sortable="true" ShowExport="true" ExportFileName="examiner-queue">
                            <ChildContent>
                                <DataColumn TItem="InterventionQueueRow" Title="Domain" Width="120px" SortKey="@(x => x.Domain)">
                                    <CellTemplate><span class="fc-badge fc-badge--neutral">@context.Domain</span></CellTemplate>
                                </DataColumn>
                                <DataColumn TItem="InterventionQueueRow" Title="Subject" SortKey="@(x => x.Subject)">
                                    <CellTemplate><strong>@context.Subject</strong></CellTemplate>
                                </DataColumn>
                                <DataColumn TItem="InterventionQueueRow" Title="Signal" Width="90px" SortKey="@(x => x.Signal)">
                                    <CellTemplate><span class="fc-badge @SignalBadge(context.Signal)">@context.Signal</span></CellTemplate>
                                </DataColumn>
                                <DataColumn TItem="InterventionQueueRow" Title="Priority" Width="100px" SortKey="@(x => x.Priority)">
                                    <CellTemplate><span class="fc-badge @PriorityBadge(context.Priority)">@context.Priority</span></CellTemplate>
                                </DataColumn>
                                <DataColumn TItem="InterventionQueueRow" Title="Next Action" SortKey="@(x => x.NextAction)">
                                    <CellTemplate>@context.NextAction</CellTemplate>
                                </DataColumn>
                                <DataColumn TItem="InterventionQueueRow" Title="Due" Width="120px" SortKey="@(x => x.DueDate)">
                                    <CellTemplate>@context.DueDate.ToString("dd MMM yyyy")</CellTemplate>
                                </DataColumn>
                            </ChildContent>
                        </DataTable>
                    }
                </div>
            </div>

            @* Recent Activity *@
            <div class="fc-card">
                <div class="fc-card__header"><h2 class="fc-card__title">Recent Activity</h2></div>
                <div class="fc-card__body">
                    <DataTable TItem="ActivityTimelineRow" Items="@_data.RecentActivity" PageSize="10" Sortable="true">
                        <ChildContent>
                            <DataColumn TItem="ActivityTimelineRow" Title="Domain" Width="120px" SortKey="@(x => x.Domain)">
                                <CellTemplate><span class="fc-badge fc-badge--neutral">@context.Domain</span></CellTemplate>
                            </DataColumn>
                            <DataColumn TItem="ActivityTimelineRow" Title="Event" SortKey="@(x => x.Title)">
                                <CellTemplate>
                                    <strong>@context.Title</strong>
                                    <div class="fc-text-muted-sm">@context.Detail</div>
                                </CellTemplate>
                            </DataColumn>
                            <DataColumn TItem="ActivityTimelineRow" Title="Severity" Width="100px" SortKey="@(x => x.Severity)">
                                <CellTemplate><span class="fc-badge @SeverityBadge(context.Severity)">@context.Severity</span></CellTemplate>
                            </DataColumn>
                            <DataColumn TItem="ActivityTimelineRow" Title="When" Width="150px" SortKey="@(x => x.HappenedAt)">
                                <CellTemplate>@context.HappenedAt.ToString("dd MMM yyyy HH:mm")</CellTemplate>
                            </DataColumn>
                        </ChildContent>
                    </DataTable>
                </div>
            </div>
        }
    </SectionErrorBoundary>
</div>

@code {
    private bool _loading = true;
    private Exception? _loadException;
    private FC.Engine.Admin.Services.Dashboards.ExaminerDashboardData? _data;

    protected override async Task OnInitializedAsync() => await ReloadAsync();

    private async Task ReloadAsync()
    {
        _loading = true; _loadException = null;
        try { _data = await DashboardService.GetExaminerDashboardAsync(); }
        catch (Exception ex) { _loadException = ex; }
        finally { _loading = false; }
    }

    private static string PriorityBadge(string p) => p switch { "Critical" => "fc-badge--danger", "High" => "fc-badge--warning", "Medium" => "fc-badge--info", _ => "fc-badge--neutral" };
    private static string SignalBadge(string s) => s switch { "Critical" or "Alert" => "fc-badge--danger", "Warning" or "Watch" => "fc-badge--warning", "Resolved" => "fc-badge--success", _ => "fc-badge--neutral" };
    private static string SeverityBadge(string s) => s switch { "Critical" => "fc-badge--danger", "High" or "Warning" => "fc-badge--warning", "Info" => "fc-badge--info", _ => "fc-badge--neutral" };
}
